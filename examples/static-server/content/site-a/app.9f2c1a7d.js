// A fingerprinted asset: the hash is in the name, so the bytes behind that name
// can never change and the server marks it immutable for a year.
document.querySelector('main')?.insertAdjacentHTML('beforeend',
    '<p class="tag">this line was added by app.9f2c1a7d.js, cached immutably</p>');
